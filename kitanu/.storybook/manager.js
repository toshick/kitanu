import { addons } from '@storybook/addons';

addons.setConfig({
  // showNav: false,
  panelPosition: 'right',
  showPanel: false,
  // isToolshown: false,
  showRoots: false,
  showAddons: false,
});
